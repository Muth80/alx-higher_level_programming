-- This script lists all records of the table second_table from the hbtn_0c_0 database
-- The results display both the score and the name columns, ordered by score (top first)
SELECT score, name
FROM 'second_table'
ORDER BY score DESC;
